float a = 10.00

// Pętla wykonuje się, dopóki 'a' jest mniejsze lub równe 20
while a <= 20.00 do

  // Kod do wykonania w pętli:
  print(a)      // Wypisz aktualną wartość 'a'
  a = a + 1   // Zwiększ 'a' o 1 (to kluczowe, aby pętla się zakończyła!)

end_while

print('Koniec pętli.')

